import { Box } from "@mui/material";
import { ElwisFtmItem } from "elwis-api";

type FtmMessageMapProps = {
  messages: ElwisFtmItem[];
};

export function FtmMessageMap({ messages }: FtmMessageMapProps) {
  return <Box></Box>;
}
